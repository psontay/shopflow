global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'shopflow-microservices'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
          - 'host.docker.internal:8888' # Gateway (cổng 8888)
          - 'host.docker.internal:8083' # Inventory